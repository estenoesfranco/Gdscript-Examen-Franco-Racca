extends Node

var location =([vector2(16,3), vector2(22,10), vector2(4,56), vector2(10,5)])
var locazo = []

func _sumLocations(location,locazo):
    var i = location.size
    var j = locazo.size
    while i > j:
        prints(vector2(4,4), vector2(44, 44))


func _ready():
    
    _sumLocations():
        prints(var location.size)
    
    pass